# Believe 微服务架构 - 项目设计文档

**版本**：v3.2
**最后更新**：2026-05-19
**维护者**：Believe Architecture Team

---

## 一、文档概述

### 1.1 项目定位

从零构建的通用型微服务架构，提供生产级基础设施、标准化开发规范和完整CI/CD交付流程。

### 1.2 核心目标

- 构建可复用的基础框架Starter集合
- 实现服务注册发现、配置管理、网关路由、限流熔断、认证授权等核心能力
- 建立完整的容器化部署和CI/CD流水线
- 深度掌握SPI、Spring Boot自动配置等底层机制

### 1.3 技术原则


| 原则         | 说明                                    |
| ------------ | --------------------------------------- |
| 约定优于配置 | 提供合理的默认值，降低使用门槛          |
| 开闭原则     | 通过SPI和条件注解支持扩展，核心逻辑封闭 |
| 可观测性优先 | 所有组件必须暴露日志、指标、链路信息    |
| 云原生友好   | 支持容器化部署和K8s编排                 |

---

## 二、技术栈总览

### 2.1 核心框架版本


| 组件                 | 版本       |
| -------------------- | ---------- |
| JDK                  | 25         |
| Spring Boot          | 4.0.6      |
| Spring Cloud         | 2025.1.0   |
| Spring Cloud Alibaba | 2025.1.0.0 |
| Maven                | 3.9+       |

### 2.2 关键基础设施


| 分类      | 组件                    | 版本   | 用途                                   |
| --------- | ----------------------- | ------ | -------------------------------------- |
| 注册/配置 | Nacos                   | 3.1.1  | 服务注册发现 + 配置管理                |
| 网关      | Spring Cloud Gateway    | 5.0.x  | API网关（WebFlux响应式）               |
| 限流熔断  | Sentinel                | 1.8.9  | 流量控制（网关层限流改用内置滑动窗口） |
| 认证授权  | Sa-Token                | 1.45.0 | 登录认证、权限管理                     |
| 任务调度  | xxl-job                 | 2.5.0  | 分布式定时任务                         |
| 链路追踪  | SkyWalking              | 9.7.x  | 分布式链路追踪                         |
| 消息队列  | RocketMQ/RabbitMQ/Kafka | —     | 三选一动态切换                         |
| ORM       | MyBatis-Plus            | 3.5.15 | 数据持久化增强                         |
| 分库分表  | ShardingSphere          | 5.5.3  | 分库分表、读写分离                     |
| 缓存      | Redis                   | 7.4.x  | 分布式缓存、分布式锁                   |
| JSON处理  | Jackson 3               | 3.1.2  | 序列化/反序列化                        |

> **网关技术栈说明**：Spring Cloud Gateway 在 Spring Boot 4.0 / Spring Cloud 2025.1.0 中 artifactId 变更为 `spring-cloud-gateway-server-webflux` (5.0.x)，不再使用旧版 `spring-cloud-starter-gateway` (4.2.x)。

### 2.3 部署与运维


| 分类     | 组件                 | 用途             |
| -------- | -------------------- | ---------------- |
| 容器化   | Docker 27.x          | 应用容器化       |
| 编排     | Kubernetes 1.32+     | 容器编排         |
| CI/CD    | GitLab CI 17.x       | 持续集成与部署   |
| 镜像仓库 | Harbor 2.11.x        | 私有镜像存储     |
| 监控     | Prometheus + Grafana | 指标采集与可视化 |
| 日志     | ELK 8.15.x           | 日志聚合分析     |

---

## 三、项目结构设计

### 3.1 顶层目录结构

```
believe-future/
├── believe-framework/                # 基础框架父模块
│   ├── pom.xml                       # 版本管理与依赖声明
│   ├── believe-common/               # 通用基础子模块集合
│   │   ├── believe-common-core       # 核心工具、统一返回、上下文
│   │   ├── believe-common-log        # 日志链路追踪、@Log注解
│   │   ├── believe-common-redis      # Redis封装、分布式锁
│   │   ├── believe-common-feign      # Feign请求拦截器
│   │   ├── believe-common-swagger    # Knife4j API文档
│   │   ├── believe-common-mq         # 消息队列封装（三选一）
│   │   ├── believe-common-security   # AES/GCM加解密
│   │   ├── believe-common-exception  # 全局异常处理
│   │   ├── believe-common-data       # 数据持久化（4种数据源模式）
│   │   ├── believe-common-spi        # SPI扩展接口定义
│   │   └── believe-common-spi-extension  # SPI默认实现
│   └── believe-job/                  # xxl-job执行器封装
├── believe-gateway/                  # API网关服务
├── believe-auth/                     # 认证授权中心
├── believe-user/                     # 用户中心
├── k8s/                              # Kubernetes部署文件
├── scripts/                          # 运维脚本
└── docs/                             # 文档
```

### 3.2 Maven模块坐标


| 模块        | ArtifactId                   |
| ----------- | ---------------------------- |
| 框架父POM   | believe-framework            |
| 核心        | believe-common-core          |
| Redis       | believe-common-redis         |
| 日志        | believe-common-log           |
| Feign       | believe-common-feign         |
| Swagger     | believe-common-swagger       |
| MQ          | believe-common-mq            |
| 安全        | believe-common-security      |
| 异常        | believe-common-exception     |
| 数据        | believe-common-data          |
| SPI接口     | believe-common-spi           |
| SPI扩展实现 | believe-common-spi-extension |
| 任务Starter | believe-job                  |
| 网关服务    | believe-gateway              |
| 认证服务    | believe-auth                 |
| 用户服务    | believe-user                 |

---

## 四、核心组件设计

### 4.1 believe-common-core

**职责**：提供最基础的工具能力和公共契约。

**核心能力**：

- `Resultt`、`PageResultt` 统一返回格式：`{code, message, data, timestamp}`
- `AuthContext`（ThreadLocal）和 `RequestContext`（ThreadLocal）上下文持有
- 异常体系：`BelieveException`、`BizException`
- 工具类：`JsonUtil`（Jackson 3）、`DateUtil`、`IdUtil`
- 错误码规范：1xxx参数错误，2xxx业务错误，3xxx系统错误，4xxx权限错误，5xxx服务调用错误

### 4.2 believe-common-redis

**职责**：Redis操作封装。

**暴露Bean**：

- `RedisTemplatestring,`（Jackson 3序列化）
- `RedisDistributedLock`（基于SET NX EX + Lua）
- `CacheManager`（Spring Cache集成，默认TTL 1小时）

### 4.3 believe-common-data

**职责**：数据持久化增强，四种数据源模式互斥激活。


| 模式     | 激活条件                                     | 说明     |
| -------- | -------------------------------------------- | -------- |
| 单数据源 | 默认                                         | 开发环境 |
| 读写分离 | `believe.data.read-write-split.enabled=true` | 主从架构 |
| 多数据源 | `believe.data.multi-datasource.enabled=true` | 多库独立 |
| 分库分表 | `believe.data.sharding.enabled=true`         | 水平拆分 |

**其他能力**：

- `@DataSource` 注解 + AOP动态切换数据源
- MyBatis-Plus分页插件、乐观锁插件
- 自动填充：`createTime`、`updateTime`、`createBy`、`updateBy`、逻辑删除
- `BaseEntity` 抽象基类
- `CustomSqlInjector` 批量插入支持

### 4.4 believe-common-spi

**职责**：SPI扩展点接口定义。


| 扩展接口         | 方法                        | 说明         |
| ---------------- | --------------------------- | ------------ |
| `NotifyChannel`  | `send` / `sendAsync`        | 通知渠道     |
| `IdGenerator`    | `nextId` / `nextIdStr`      | 分布式ID生成 |
| `DataSerializer` | `serialize` / `deserialize` | 数据序列化   |

**加载机制**：Java原生 `ServiceLoader` + Spring `@ConditionalOnMissingBean`。

### 4.5 believe-common-spi-extension

**职责**：为上述扩展点提供生产级默认实现。

#### 通知渠道


| 实现                    | 激活条件                                           | 说明       |
| ----------------------- | -------------------------------------------------- | ---------- |
| `SmsNotifyChannel`      | `believe.spi.notify.sms.api-url`                   | 短信网关   |
| `EmailNotifyChannel`    | `JavaMailSender` + `believe.spi.notify.email.from` | 邮件发送   |
| `DingTalkNotifyChannel` | `believe.spi.notify.dingtalk.webhook-url`          | 钉钉机器人 |

#### 分布式ID生成器


| 实现                     | 算法         | 激活条件                                     |
| ------------------------ | ------------ | -------------------------------------------- |
| `SnowflakeIdGenerator`   | 雪花算法     | 默认启用                                     |
| `LeafSegmentIdGenerator` | 美团Leaf号段 | `DataSource` + `believe.spi.id.leaf.biz-tag` |

#### 序列化器


| 实现                     | 格式              | 激活条件                  |
| ------------------------ | ----------------- | ------------------------- |
| `JsonDataSerializer`     | JSON（Jackson 3） | 默认启用                  |
| `ProtobufDataSerializer` | Protobuf          | `MessageLite` 在classpath |

**覆盖机制**：所有Bean均通过 `@ConditionalOnMissingBean` 注册，业务方可自定义替换。

**配置前缀**：`believe.spi`


| 配置项                        | 示例                                                    |
| ----------------------------- | ------------------------------------------------------- |
| `notify.sms.api-url`          | `https://sms.example.com/send`                          |
| `notify.email.from`           | `noreply@believe.com`                                   |
| `notify.dingtalk.webhook-url` | `https://oapi.dingtalk.com/robot/send?access_token=xxx` |
| `id.snowflake.worker-id`      | `1`                                                     |
| `id.snowflake.datacenter-id`  | `1`                                                     |
| `id.leaf.biz-tag`             | `order_id`                                              |
| `id.leaf.step`                | `2000`                                                  |

### 4.6 believe-common-exception

**职责**：全局异常统一处理，错误码→HTTP状态码映射。

- `BelieveException` → 按错误码分类（1xxx/2xxx→400，3xxx→500，4xxx→401/403，5xxx→502）
- 参数校验异常 → 400
- 404 → 404
- 兜底 → 500

### 4.7 believe-common-log

**职责**：日志链路追踪与AOP方法日志。

- `@Log` 注解：记录入参、耗时、异常
- `TraceIdFilter`：提取/生成TraceId，写入MDC和RequestContext

### 4.8 believe-common-feign

**职责**：Feign远程调用增强。

- `FeignRequestInterceptor`：自动注入 `X-Trace-Id`、`X-User-Id`、`X-Username`、`Authorization`

### 4.9 believe-common-swagger

**职责**：Knife4j API文档自动配置。

### 4.10 believe-common-mq

**职责**：消息队列统一封装，支持RocketMQ/RabbitMQ/Kafka三选一，通过 `believe.mq.type` 切换。

### 4.11 believe-common-security

**职责**：`AesUtil`（AES 256位 GCM加解密）。

### 4.12 believe-gateway（已实现）

**职责**：统一流量入口，基于 Spring Cloud Gateway 5.0 WebFlux 响应式架构，集成服务发现、认证鉴权、限流、灰度与链路追踪。

**过滤器链**（按 Order 顺序执行）：


| Order | 过滤器                    | 职责                                                                                                                 |
| ----- | ------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| -2    | `AuthGlobalFilter`        | 解析 JWT，白名单（`/auth/**`、`/actuator/**`）放行，其余请求调用 Sa-Token 校验登录状态，通过后向下游传递 `X-User-Id` |
| -1    | `TraceIdGlobalFilter`     | 提取或生成`X-Trace-Id`，写入 MDC 并传递至下游服务                                                                    |
| 0     | `RateLimitGlobalFilter`   | 基于滑动窗口算法实现 QPS 限流（可配置），超限返回 HTTP 429                                                           |
| 1     | `GrayReleaseGlobalFilter` | 检测`X-Gray-Release` 请求头，标记灰度版本，用于灰度路由                                                              |
| 5     | `RequestLogGlobalFilter`  | 记录请求摘要日志，格式：`[traceId] METHOD path status duration`                                                      |

**路由规范**：

- 标准路由：`/api/{service-name}/**` → `lb://{service-name}`，自动去除 `/api` 前缀
- 白名单路径：`/auth/**`、`/actuator/**` 绕过认证

**认证失败响应**：返回统一 JSON 格式，错误码 4001，消息“未登录或 Token 已过期”。

**限流实现说明**：由于 Sentinel 5.0 移除了 `GatewayCallbackManager`，网关层不再使用 Sentinel 的网关限流适配器，改为自定义 `RateLimitGlobalFilter` + 滑动窗口计数器，通过 `believe.gateway.rate-limit.qps` 配置阈值。

**主要配置**：

- `believe.gateway.rate-limit.enabled`：是否启用限流（默认 true）
- `believe.gateway.rate-limit.qps`：每秒允许请求数（默认 100）

### 4.13 believe-auth（已实现）

**职责**：认证授权中心，提供用户登录、Token 颁发、Token 校验及 RBAC 权限数据管理。

**数据模型**：经典 5 表 RBAC 结构

- `sys_user`：用户表（username, password(salt:hash), status）
- `sys_role`：角色表（name, code）
- `sys_permission`：权限表（name, code, type：菜单/按钮/API）
- `sys_user_role`：用户-角色关联表
- `sys_role_permission`：角色-权限关联表

**核心 API**：


| 方法 | 路径              | 说明                                   | 认证 |
| ---- | ----------------- | -------------------------------------- | ---- |
| POST | `/auth/login`     | 用户名密码登录，返回 JWT（RS256 签名） | 否   |
| POST | `/auth/register`  | 新用户注册                             | 否   |
| POST | `/auth/logout`    | 登出（Token 黑名单）                   | 是   |
| GET  | `/auth/user-info` | 获取当前用户信息及角色、权限列表       | 是   |

**与网关协作**：`believe-gateway` 的 `AuthGlobalFilter` 调用 `StpUtil.checkLogin()` 完成认证，网关不重复执行权限查询；权限数据通过 `StpInterfaceImpl` 从数据库加载，供下游服务使用。

**密码安全**：采用 BCrypt 加密存储。

**JWT 密钥**：使用非对称加密（RS256），私钥仅 auth 服务持有，公钥供网关校验。

### 4.14 believe-user（已实现）

**职责**：用户中心服务，提供用户个人资料管理、管理员分页查询及用户信息维护接口。依赖 `believe-auth` 提供的认证与权限数据。

**核心 API**：


| 方法   | 路径         | 说明                                     | 权限要求      |
| ------ | ------------ | ---------------------------------------- | ------------- |
| GET    | `/user/info` | 获取当前登录用户信息                     | 登录即可      |
| PUT    | `/user/info` | 更新当前用户资料（昵称、邮箱、手机号等） | 登录即可      |
| GET    | `/user/{id}` | 查询指定用户详情                         | `user:manage` |
| GET    | `/user/list` | 分页查询用户列表（支持关键词）           | `user:manage` |
| DELETE | `/user/{id}` | 删除用户                                 | `user:manage` |

**权限控制**：使用 Sa-Token 注解 `@SaCheckPermission("user:manage")` 声明权限要求。权限数据由 `believe-auth` 模块通过 `StpInterface` 提供，`believe-user` 无需重复查询数据库。

**数据层**：基于 `believe-common-data` 模块，使用 MyBatis-Plus 访问用户表（`sys_user`），字段映射符合 `BaseEntity` 规范（自动填充创建时间、更新时间等）。

**与网关协作**：网关认证通过后，将 `X-User-Id` 传递给 `believe-user`，服务通过 `RequestContext` 获取当前用户 ID，实现“只能修改本人资料”的业务规则。

### 4.15 believe-job（已实现）

**职责**：xxl-job 执行器自动配置模块，引入即自动注册执行器，并提供任务级链路追踪能力。

**自动配置**：根据 `believe.job.enabled` 条件创建 `XxlJobSpringExecutor` 并注册到调度中心。只需配置调度中心地址、执行器名称、端口等即可使用，无需额外 Java 配置。

**链路追踪**：提供 `JobTraceHelper` 工具类，在任务方法入口调用 `initTrace(taskName)` 生成 TraceId 并写入 `RequestContext` 和 MDC，任务结束时调用 `clearTrace()`。该 TraceId 会通过 `believe-common-feign` 自动传递到下游服务，实现全链路串联。

**核心配置**（`believe.job.*`）：


| 配置项               | 默认值                              | 说明             |
| -------------------- | ----------------------------------- | ---------------- |
| `enabled`            | true                                | 是否启用执行器   |
| `admin-addresses`    | http://127.0.0.1:8080/xxl-job-admin | 调度中心地址     |
| `app-name`           | believe-job-executor                | 执行器应用名     |
| `access-token`       | default_token                       | 通信令牌         |
| `port`               | 9999                                | 执行器监听端口   |
| `log-path`           | /data/applogs/xxl-job/jobhandler    | 任务日志存储路径 |
| `log-retention-days` | 30                                  | 日志保留天数     |

**关闭方式**：设置 `believe.job.enabled=false` 即可，适用于本地开发环境。

---

## 五、CI/CD设计

### 5.1 流水线阶段

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Build   │ -> │  Image   │ -> │  Deploy  │ -> │   Test   │ -> │  Deploy  │
│ (编译)    │    │ (镜像)    │    │  (开发)   │    │ (测试)    │    │ (生产)    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
   Maven          Docker          K8s Dev        Integration      K8s Prod
   Unit Test      Push to         Auto            Performance     Manual
   SonarQube      Harbor                                          Approval
```

### 5.2 环境与分支


| 环境    | 分支      | 部署方式 |
| ------- | --------- | -------- |
| dev     | develop   | 自动     |
| staging | release/* | 自动     |
| prod    | main      | 手动审批 |

### 5.3 制品规范

镜像命名：`registry.believe.com/believe/{service-name}:{git-commit-sha}`
版本标签：`latest`、`v{major}.{minor}.{patch}`

---

## 六、配置规范

### 6.1 配置文件层级

`bootstrap.yml` → `application.yml` → `application-{env}.yml` → Nacos配置中心（最高优先级）

### 6.2 Nacos规划


| Namespace | 用途     | 配置集示例                                    |
| --------- | -------- | --------------------------------------------- |
| dev       | 开发环境 | `user-service-dev.yaml`                       |
| test      | 测试环境 | `user-service-test.yaml`                      |
| prod      | 生产环境 | `user-service-prod.yaml`                      |
| common    | 公共配置 | `common-redis.yaml`、`common-datasource.yaml` |

---

## 七、服务端口规划


| 服务               | 端口  | 管理端口 |
| ------------------ | ----- | -------- |
| believe-gateway    | 8080  | 8081     |
| believe-auth       | 8101  | 8102     |
| believe-user       | 8201  | 8202     |
| Nacos              | 8848  | 9848     |
| Sentinel Dashboard | 8858  | -        |
| xxl-job-admin      | 8888  | -        |
| SkyWalking OAP     | 11800 | 12800    |

---

## 八、部署拓扑

### 开发环境（Docker Compose单机）

MySQL:3306、Redis:6379、Nacos:8848、Sentinel:8858、xxl-job:8888、ES:9200

### 生产环境（K8s）

- Ingress-Nginx（LB）→ 服务网格（可选Istio）→ 微服务Pod（多副本）
- 有状态服务：MySQL、Redis、Nacos、ES（StatefulSet）
- 监控：Prometheus + Grafana

---

## 九、Maven关键配置

### 9.1 Framework父POM显式版本管理


| 组件             | 版本    |
| ---------------- | ------- |
| MyBatis-Plus     | 3.5.15  |
| ShardingSphere   | 5.5.3   |
| Sa-Token         | 1.45.0  |
| xxl-job          | 2.5.0   |
| Knife4j          | 4.5.0   |
| MySQL Connector  | 9.3.0   |
| RocketMQ Starter | 2.3.5   |
| Lombok           | 1.18.46 |

### 9.2 特殊配置

- JDK 25注解处理需在 `maven-compiler-plugin` 中显式配置 `annotationProcessorPaths` 指定Lombok版本
- 三种MQ依赖均为 `optionaltrue`
- SPI扩展实现模块中 `spring-boot-starter-mail` 和 `protobuf-java` 为optional

---

## 十、关键版本说明

### v3.2 相对于 v3.1 的核心变化

1. **网关模块实现细化**

   - 明确 5 个 WebFilter 的执行顺序与职责。
   - 限流实现从“Sentinel 限流”修正为内置滑动窗口（因 Sentinel 5.0 移除了 `GatewayCallbackManager`）。
   - 更新 Spring Cloud Gateway 版本为 5.0.x，并说明 artifactId 变更。
   - 补充认证失败响应格式与限流配置。
2. **认证模块实现细化**

   - 补充 RBAC 5 表模型、核心 API 列表及 JWT 签名方式（RS256）。
   - 说明与网关的认证协作方式（网关调用 `StpUtil.checkLogin()`）。
   - 明确密码加密与权限数据加载机制。
3. **新增用户中心模块**

   - 增加 `believe-user` 的服务职责、API 列表与权限要求。
   - 说明依赖 `believe-auth` 提供权限数据，以及与网关的上下文传递。
4. **定时任务模块实现细化**

   - 补充 `believe-job` 的自动配置机制、`JobTraceHelper` 链路追踪原理。
   - 列出核心配置项及关闭方式。

### Spring Boot 4.0 重要提醒

1. Servlet API 升级为 `jakarta.servlet.*`（Servlet 6.1）
2. Jackson 3 核心包名变更为 `tools.jackson.*`，使用 `JsonMapper.builder()`
3. 无独立 `spring-boot-starter-aop`，需直接引入 `spring-aop` 和 `aspectjweaver`
4. 日期配置使用 `DateTimeFeature.WRITE_DATES_AS_TIMESTAMPS`

---

## 十一、文档版本记录


| 版本      | 日期           | 变更内容                                                                                                         |
| --------- | -------------- | ---------------------------------------------------------------------------------------------------------------- |
| v1.0      | 2026-05-18     | 初始版本（JDK17+SB3.2）                                                                                          |
| v2.0      | 2026-05-18     | 升级至JDK25+SB4.0.6+SCA2025.1.0.0                                                                                |
| v2.1~v2.8 | 2026-05-18     | 结构重构、版本精简、Jackson 3集成、数据层重构                                                                    |
| v2.9      | 2026-05-18     | 完成log/redis/exception/feign/swagger/mq/security 7个模块                                                        |
| v3.0      | 2026-05-18     | 新增SPI扩展实现模块                                                                                              |
| v3.1      | 2026-05-18     | 整体精简压缩，保持核心信息完整，流水线阶段保留ASCII图                                                            |
| **v3.2**  | **2026-05-19** | **增量更新：补充 gateway、auth、user、job 四个已实现模块的详细设计、技术决策与配置说明，修正网关版本与限流方案** |

---

*本文档为架构设计文档，详细实现请参考各模块开发指南与源码。*
