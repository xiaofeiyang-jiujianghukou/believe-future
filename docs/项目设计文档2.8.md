# Believe 微服务架构 - 项目设计文档

**版本**：v2.8
**最后更新**：2026-05-18
**维护者**：Believe Architecture Team

---

## 一、文档概述

### 1.1 项目定位

Believe 是一套从零构建的通用型微服务架构，旨在提供生产级的基础设施能力、标准化的开发规范和完整的CI/CD交付流程。

### 1.2 核心目标

- 构建可复用的基础框架Starter集合
- 实现服务注册发现、配置管理、网关路由、限流熔断、认证授权等核心能力
- 建立完整的容器化部署和CI/CD流水线
- 深度掌握SPI、Spring Boot自动配置等底层机制

### 1.3 技术原则


| 原则         | 说明                                    |
| ------------ | --------------------------------------- |
| 约定优于配置 | 提供合理的默认值，降低使用门槛          |
| 开闭原则     | 通过SPI和条件注解支持扩展，核心逻辑封闭 |
| 可观测性优先 | 所有组件必须暴露日志、指标、链路信息    |
| 云原生友好   | 支持容器化部署和K8s编排                 |

---

## 二、技术栈总览

### 2.1 核心框架版本


| 组件                 | 版本       | 说明                       |
| -------------------- | ---------- | -------------------------- |
| JDK                  | 25         | 最新版本，支持最新语言特性 |
| Spring Boot          | 4.0.6      | 核心框架，支持AOT和GraalVM |
| Spring Cloud         | 2025.1.0   | 微服务生态                 |
| Spring Cloud Alibaba | 2025.1.0.0 | 阿里微服务组件集合         |
| Maven                | 3.9+       | 构建工具                   |

### 2.2 基础设施组件


| 分类       | 组件                 | 版本             | 用途                                        |
| ---------- | -------------------- | ---------------- | ------------------------------------------- |
| 注册/配置  | Nacos                | 3.1.1            | 服务注册发现 + 配置管理                     |
| 网关       | Spring Cloud Gateway | 4.2.x            | API网关，路由/过滤/限流                     |
| 限流熔断   | Sentinel             | 1.8.9            | 流量控制、熔断降级                          |
| 认证授权   | Sa-Token             | 1.45.0           | 登录认证、权限管理、SSO                     |
| 任务调度   | xxl-job              | 2.5.0            | 分布式定时任务                              |
| 链路追踪   | SkyWalking           | 9.7.x            | 分布式链路追踪、性能分析                    |
| 分布式事务 | Seata                | 2.5.0            | AT模式分布式事务                            |
| 消息队列   | RocketMQ             | 5.3.1            | 异步消息、削峰填谷                          |
| 数据持久   | MyBatis-Plus         | 3.5.15           | ORM增强，简化CRUD                           |
| 分库分表   | ShardingSphere       | 5.5.3            | 分库分表、读写分离                          |
| 连接池     | HikariCP             | 6.2.x            | 数据库连接池                                |
| 缓存       | Redis                | 7.4.x            | 分布式缓存、分布式锁                        |
| JSON处理   | Jackson 3            | 3.1.2            | JSON序列化/反序列化（Spring Boot 4.0 默认） |
| AOP        | Spring AOP + AspectJ | 7.0.7 / 1.9.25.1 | 面向切面编程支持                            |

### 2.3 部署与运维


| 分类     | 组件                 | 版本           | 用途               |
| -------- | -------------------- | -------------- | ------------------ |
| 容器化   | Docker               | 27.x           | 应用容器化         |
| 编排     | Kubernetes           | 1.32+          | 容器编排、弹性伸缩 |
| 网关入口 | Ingress-Nginx        | 1.12.x         | K8s流量入口        |
| CI/CD    | GitLab CI            | 17.x           | 持续集成与持续部署 |
| 镜像仓库 | Harbor               | 2.11.x         | 私有镜像存储       |
| 监控     | Prometheus + Grafana | 3.0.x + 11.5.x | 指标采集与可视化   |
| 日志     | ELK                  | 8.15.x         | 日志聚合、搜索分析 |
| 代码质量 | SonarQube            | 10.7.x         | 代码静态扫描       |

---

## 三、项目结构设计

### 3.1 顶层目录结构

```
believe-future/                       # 项目根目录（纯目录，无pom.xml）
│
├── believe-framework/                # 基础框架模块（独立模块，管理所有版本依赖）
│   ├── pom.xml                       # framework父POM（版本管理与依赖声明）
│   ├── believe-common/               # 通用基础子模块集合
│   │   ├── believe-common-core       # 核心工具、Result/PageResult、异常、上下文、JsonUtil
│   │   ├── believe-common-log        # 日志链路追踪、MDC、AOP切面
│   │   ├── believe-common-redis      # Redis操作封装、分布式锁
│   │   ├── believe-common-feign      # Feign统一配置、熔断fallback
│   │   ├── believe-common-swagger    # API文档自动配置
│   │   ├── believe-common-mq         # 消息队列基础封装
│   │   ├── believe-common-security   # 加解密、JWT工具
│   │   ├── believe-common-exception  # 全局异常处理器
│   │   ├── believe-common-data       # 数据持久化（多数据源/读写分离/分库分表/自动填充）
│   │   └── believe-common-spi        # SPI扩展机制基础定义
│   │
│   └── believe-job/                  # xxl-job执行器封装（Starter）
│
├── believe-gateway/                  # API网关服务
├── believe-auth/                     # 认证授权中心（含RBAC）
├── believe-user/                     # 用户中心
│
├── k8s/                              # Kubernetes部署文件
│   ├── base/                         # 基础配置
│   ├── dev/                          # 开发环境
│   ├── staging/                      # 预发环境
│   └── prod/                         # 生产环境
│
├── scripts/                          # 运维脚本
│   ├── deploy.sh                     # 部署脚本
│   └── init-db.sql                   # 数据库初始化
│
└── docs/                             # 文档
    ├── architecture.md               # 架构设计文档
    └── api/                          # API文档聚合
```

### 3.2 逻辑分层说明

```
┌─────────────────────────────────────────────────────────────────┐
│                     应用服务层（独立微服务）                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Gateway     │  │ Auth        │  │ User        │              │
│  │ (独立进程)   │  │ (独立进程)   │  │ (独立进程)   │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│         │                │                │                      │
│         └────────────────┼────────────────┘                      │
│                          ▼                                       │
├─────────────────────────────────────────────────────────────────┤
│                   基础框架层（jar包依赖）                          │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    believe-framework                     │    │
│  │              （统一版本管理 + 依赖声明）                   │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌──────┐  │    │
│  │  │ core   │ │ redis  │ │ log    │ │ feign  │ │ data │  │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └──────┘  │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐           │    │
│  │  │ swagger│ │ mq     │ │security│ │ spi    │           │    │
│  │  └────────┘ └────────┘ └────────┘ └────────┘           │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 Maven模块坐标


| 模块           | GroupId     | ArtifactId           | 说明                                |
| -------------- | ----------- | -------------------- | ----------------------------------- |
| Framework父POM | com.believe | believe-framework    | 基础框架父POM，管理所有版本依赖     |
| 核心           | com.believe | believe-common-core  | 纯工具、统一返回、异常、上下文      |
| Redis          | com.believe | believe-common-redis | 依赖core，提供缓存能力              |
| 日志           | com.believe | believe-common-log   | 依赖core，提供日志切面              |
| Feign          | com.believe | believe-common-feign | 依赖core，提供远程调用增强          |
| 数据           | com.believe | believe-common-data  | 依赖core，数据持久化、多数据源      |
| SPI            | com.believe | believe-common-spi   | SPI扩展点定义                       |
| 任务Starter    | com.believe | believe-job          | xxl-job执行器封装                   |
| 网关服务       | com.believe | believe-gateway      | 独立启动服务                        |
| 认证服务       | com.believe | believe-auth         | 独立启动服务                        |
| 用户服务       | com.believe | believe-user         | 独立启动服务                        |

### 3.4 模块依赖关系

```
┌─────────────────────────────────────────────────────────────────┐
│                    believe-framework/pom.xml                    │
│                   （统一版本管理 + dependencyManagement）         │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    Maven模块聚合                          │    │
│  │                                                          │    │
│  │    believe-common-core (基础层，引入Lombok、Jackson 3)    │    │
│  │            ↑                                             │    │
│  │    ┌───────┼───────┬───────────┐                         │    │
│  │    ↓       ↓       ↓           ↓                         │    │
│  │  redis    log    feign        data                       │    │
│  │  swagger  mq    security     (含ShardingSphere、AOP)     │    │
│  │                             spi                          │    │
│  │                                                          │    │
│  │    believe-job (Starter，可选依赖)                        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 依赖（通过Maven引入）
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      独立微服务模块                               │
│                                                                  │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│   │believe-gateway│  │believe-auth  │  │believe-user  │          │
│   │  (独立进程)   │  │  (独立进程)   │  │  (独立进程)   │          │
│   │              │  │              │  │              │          │
│   │ 引入framework │  │ 引入framework │  │ 引入framework │          │
│   │ 中的common    │  │ 中的common    │  │ 中的common    │          │
│   └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、核心组件设计

### 4.1 believe-common-core

**职责**：提供最基础的工具能力和公共契约，是整个框架的唯一底层依赖。


| 包路径                              | 功能         | 核心类                                                |
| ----------------------------------- | ------------ | ----------------------------------------------------- |
| `com.believe.common.core.result`    | 统一返回结果 | `Result<T>`、`PageResult<T>`                          |
| `com.believe.common.core.constants` | 系统常量     | `ServiceConstants`、`HeaderConstants`、`ErrorCode`    |
| `com.believe.common.core.utils`     | 通用工具类   | `JsonUtil`（Jackson 3）、`DateUtil`、`IdUtil`         |
| `com.believe.common.core.exception` | 基础异常定义 | `BelieveException`、`BizException`                    |
| `com.believe.common.core.context`   | 上下文持有者 | `AuthContext`、`RequestContext`                       |

**核心契约定义**：

- 统一响应格式：`{ code, message, data, timestamp }`
- 错误码规范：1xxx参数错误，2xxx业务错误，3xxx系统错误，4xxx权限错误，5xxx服务调用错误
- 透传Header约定：`X-Trace-Id`、`X-User-Id`、`X-Username`、`X-Authorities`

**依赖引入**：

| 依赖 | GroupId | 说明 |
|------|---------|------|
| `jackson-databind` | `tools.jackson.core` | Jackson 3 核心（scope=provided，由下游提供） |
| `jackson-annotations` | `com.fasterxml.jackson.core` | Jackson 注解（包名未迁移，scope=provided） |
| `lombok` | `org.projectlombok` | 简化代码，减少样板方法（scope=provided） |

#### 4.1.1 Result / PageResult

- 统一使用 `Result<T>` 和 `PageResult<T>` 作为返回体
- 基于 Lombok `@Data` + `@Accessors(chain=true)`，支持链式调用
- `Result` 提供静态工厂方法：`ok()`、`ok(data)`、`error(code, message)`
- `Result` 提供快捷判断：`isSuccess()`、`isError()`
- `PageResult` 提供：`of(total, records)`、`empty()`、`hasData()`、`getTotalPages()`

#### 4.1.2 AuthContext（认证上下文）

- **设计模式**：实例式 ThreadLocal，通过 `AuthContext.get()` 获取当前线程实例
- **链式设置**：`setUserId()`、`setUsername()`、`setToken()`、`setAuthorities()`
- **值获取**：`getUserId()`、`getUsername()`、`getToken()`、`getAuthorities()`
- **便捷判断**：`isLogin()` — 判断当前请求是否已认证
- **调试支持**：`getAll()` — 返回所有上下文数据的快照

#### 4.1.3 RequestContext（请求上下文）

- **设计模式**：实例式 ThreadLocal，通过 `RequestContext.get()` 获取当前线程实例
- **链式设置**：`setTraceId()`、`setClientIp()`、`setUserAgent()`
- **值获取**：`getTraceId()`、`getClientIp()`、`getUserAgent()`
- **便捷判断**：`hasTraceId()` — 判断是否已设置链路追踪ID

#### 4.1.4 异常体系

- `BelieveException`：基础异常类，使用 Lombok `@Getter`，含 `code` + `message`
- `BizException`：业务异常，继承 `BelieveException`，默认错误码 `BIZ_GENERAL`
- 构造函数支持传入 `Throwable cause` 以保留异常链

#### 4.1.5 JsonUtil（Jackson 3 实现）

- 核心包名：`tools.jackson`（非 `com.fasterxml.jackson`）
- 构建方式：`JsonMapper.builder()` 替代 `new ObjectMapper()`
- 日期配置：`DateTimeFeature.WRITE_DATES_AS_TIMESTAMPS` 替代旧 API
- 特性：美化输出、忽略未知属性
- 注解包名 `com.fasterxml.jackson.annotation` 保持不变，`@JsonProperty`、`@JsonIgnore`、`@JsonFormat` 正常使用

#### 4.1.6 DateUtil / IdUtil

- `DateUtil`：基于 `java.time` 的日期工具，支持格式化、解析、`startOfDay`/`endOfDay` 等
- `IdUtil`：提供 `uuid()`（无横线）、`uuidWithDash()`（带横线）、`simpleId()`（自增，仅开发测试用）

### 4.2 believe-common-redis (Starter)

**职责**：封装Redis操作，提供开箱即用的缓存能力

**自动配置触发条件**：`RedisTemplate` 存在于classpath

**暴露的Bean**：


| Bean名称               | 类型                   | 说明              |
| ---------------------- | ---------------------- | ----------------- |
| `redisTemplate`        | `RedisTemplate<String,` | Jackson序列化配置 |
| `redisDistributedLock` | `RedisDistributedLock` | 分布式锁实现      |
| `cacheManager`         | `CacheManager`         | Spring Cache集成  |

**配置项** (`believe.redis.*`)：


| 配置项            | 默认值    | 说明             |
| ----------------- | --------- | ---------------- |
| `host`            | localhost | Redis主机地址    |
| `port`            | 6379      | Redis端口        |
| `database`        | 0         | 数据库索引       |
| `pool.max-active` | 8         | 连接池最大连接数 |
| `pool.max-idle`   | 8         | 连接池最大空闲数 |

### 4.3 believe-common-data (Starter)

**职责**：数据持久化增强，统一管理数据源配置、动态路由、自动填充、分页等能力。

**自动配置触发条件**：`DataSource` 和 `MybatisPlusAutoConfiguration` 存在

#### 4.3.1 四种数据源模式

通过配置属性按需激活，互斥生效：


| 模式           | 激活条件                                         | 说明                             |
| -------------- | ------------------------------------------------ | -------------------------------- |
| **单数据源**   | 默认（其他模式均未开启时）                       | `SingleDataSourceConfig`         |
| **读写分离**   | `believe.data.read-write-split.enabled=true`     | 一主一从，读走从库、写走主库     |
| **多数据源**   | `believe.data.multi-datasource.enabled=true`     | 支持 master / slave / report 等  |
| **分库分表**   | `believe.data.sharding.enabled=true`             | ShardingSphere 接管，YAML 驱动   |

**架构设计**：

- 所有模式继承 `BaseDataSourceConfig` 抽象类
- `BaseDataSourceConfig.createDataSource()` 统一入口，根据 `isDynamicRouting()` 决定是否启用动态路由
- 动态路由基于 `AbstractRoutingDataSource`（`DynamicDataSource`），配合 `LazyConnectionDataSourceProxy` 延迟获取连接
- `DataAutoConfiguration` 通过 `@Import` 统一注册所有模式配置，由各 `@ConditionalOnProperty` 控制激活

#### 4.3.2 @DataSource 注解与 AOP 动态切换

- `@DataSource("slave")` 注解用于方法或类级别，声明式切换数据源
- `DataSourceAspect`（AspectJ 切面，Order=1）在方法执行前设置、执行后清理
- `DynamicDataSourceContextHolder`（ThreadLocal）存储当前线程的数据源标识

#### 4.3.3 MyBatis-Plus 配置

**MybatisPlusConfig** 提供：


| 组件                   | 说明                                         |
| ---------------------- | -------------------------------------------- |
| `MybatisPlusInterceptor` | 注册分页插件（`PaginationInnerInterceptor`）和乐观锁插件（`OptimisticLockerInnerInterceptor`） |
| `MetaObjectHandler`    | 自动填充处理器，可通过 `believe.data.mybatis-plus.auto-fill=false` 关闭 |

**Mapper 扫描**：自动扫描 `com.believe.**.mapper` 路径。

#### 4.3.4 自动填充规范

`MybatisMetaObjectHandler` 实现：

- `createTime`：插入时填充 `LocalDateTime.now()`
- `updateTime`：插入和更新时填充 `LocalDateTime.now()`
- `createBy`：从 `AuthContext.get().getUserId()` 获取，未登录时默认 `"system"`
- `updateBy`：从 `AuthContext.get().getUserId()` 获取，未登录时默认 `"system"`
- `deleted`：插入时默认 `0`

#### 4.3.5 BaseEntity（基础实体）

抽象基类，所有实体继承后自动获得：


| 字段         | 注解                              | 说明                           |
| ------------ | --------------------------------- | ------------------------------ |
| `id`         | `@TableId(type=IdType.AUTO)`      | 自增主键                       |
| `createTime` | `@TableField(fill=FieldFill.INSERT)` | 创建时间（自动填充）         |
| `updateTime` | `@TableField(fill=FieldFill.INSERT_UPDATE)` | 更新时间（自动填充） |
| `createBy`   | `@TableField(fill=FieldFill.INSERT)` | 创建人（自动填充）           |
| `updateBy`   | `@TableField(fill=FieldFill.INSERT_UPDATE)` | 更新人（自动填充）     |
| `deleted`    | `@TableLogic`                     | 逻辑删除标记（0=正常，1=已删除） |

#### 4.3.6 CustomSqlInjector

继承 `DefaultSqlInjector`，注入 `InsertBatchSomeColumn` 批量插入方法。

#### 4.3.7 依赖引入

| 依赖 | 说明 |
|------|------|
| `mybatis-plus-spring-boot4-starter` | ORM 增强 |
| `mybatis-plus-jsqlparser` | 分页 SQL 解析 |
| `mysql-connector-j` | MySQL 驱动（runtime） |
| `postgresql` | PostgreSQL 驱动（runtime） |
| `shardingsphere-jdbc` | 分库分表核心 |
| `snakeyaml` | YAML 解析（ShardingSphere 规则） |
| `spring-aop` + `aspectjweaver` | AOP 数据源切换 |
| `lombok` | 简化实体类代码（provided） |

### 4.4 believe-common-spi

**职责**：定义SPI扩展点，实现插件化架构

**SPI扩展点定义**：


| 扩展接口         | 方法                      | 说明                             |
| ---------------- | ------------------------- | -------------------------------- |
| `DataSerializer` | `serialize`/`deserialize` | 数据序列化扩展                   |
| `NotifyChannel`  | `send`                    | 通知渠道扩展（短信/邮件/站内信） |
| `IdGenerator`    | `nextId` / `nextIdStr`    | 分布式ID生成扩展                 |

**加载机制**：使用Java原生 `ServiceLoader`，配合Spring Boot `@ConditionalOnMissingBean` 实现平滑覆盖

### 4.5 believe-gateway

**职责**：统一流量入口，路由转发、认证鉴权、限流熔断

**核心过滤器链**（按Order排序）：


| Order | 过滤器名称                | 职责                      |
| ----- | ------------------------- | ------------------------- |
| -2    | `AuthGlobalFilter`        | JWT解析，用户上下文注入   |
| -1    | `TraceIdGlobalFilter`     | 生成/传递TraceId，日志MDC |
| 0     | `RateLimitGlobalFilter`   | Sentinel限流检测          |
| 1     | `GrayReleaseGlobalFilter` | 灰度路由决策              |
| 5     | `RequestLogGlobalFilter`  | 请求日志记录              |

**路由配置规范**：

- 路径前缀：`/api/{service-name}/**` 映射到对应微服务
- 白名单：`/auth/**`、`/actuator/**` 绕过认证

**限流资源配置**：

- 默认每API 100 QPS
- 按用户维度每用户每API 20 QPS

### 4.6 believe-auth（认证中心）

**职责**：用户认证、Token颁发、权限管理

**核心接口**：


| 接口路径            | 方法 | 说明                        |
| ------------------- | ---- | --------------------------- |
| `/auth/login`       | POST | 账号密码登录，返回JWT Token |
| `/auth/logout`      | POST | 登出，黑名单失效Token       |
| `/auth/refresh`     | POST | 刷新Token                   |
| `/auth/check`       | GET  | 校验Token有效性             |
| `/auth/permissions` | GET  | 获取当前用户权限列表        |

**RBAC模型**（5表结构）：


| 表名              | 说明                  |
| ----------------- | --------------------- |
| `user`            | 用户基本信息          |
| `role`            | 角色定义              |
| `permission`      | 权限定义（资源+操作） |
| `user_role`       | 用户-角色关联         |
| `role_permission` | 角色-权限关联         |

**Token设计**：

- 算法：RS256（非对称加密）
- 有效期：2小时
- Refresh Token有效期：7天
- Payload包含：`userId`, `username`, `authorities`, `exp`

### 4.7 believe-common-log

**职责**：提供日志链路追踪能力，包含MDC和AOP切面

**核心实现**：

- 基于 MDC 的 TraceId 自动生成与传递
- 基于 Spring AOP + AspectJ 的方法执行日志切面
- 支持 Controller、Service 层日志自动记录

**依赖配置**：

- 不直接使用 `spring-boot-starter-aop`（不存在独立 `4.0.6` 版本）
- 直接引入 `spring-aop` + `aspectjweaver`，版本由 BOM 自动管理
- 引入 `jakarta.servlet-api`（scope=provided），用于 MDC 和 Web 切面

### 4.8 believe-job

**职责**：xxl-job执行器封装，简化任务开发

**自动配置触发条件**：`XxlJobConfiguration` 条件判断

**暴露能力**：

- 自动注册执行器到调度中心
- 提供 `@BelieveXxlJob` 注解增强（自动记录执行日志、异常告警）

**配置项**：


| 配置项                     | 说明         |
| -------------------------- | ------------ |
| `xxl.job.admin.addresses`  | 调度中心地址 |
| `xxl.job.executor.appname` | 执行器应用名 |
| `xxl.job.executor.port`    | 执行器端口   |

---

## 五、CI/CD设计

### 5.1 流水线阶段

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Build   │ -> │  Image   │ -> │  Deploy  │ -> │   Test   │ -> │  Deploy  │
│ (编译)    │    │ (镜像)    │    │  (开发)   │    │ (测试)    │    │ (生产)    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
   Maven          Docker          K8s Dev        Integration      K8s Prod
   Unit Test      Push to         Auto            Performance     Manual
   SonarQube      Harbor                                          Approval
```

### 5.2 环境划分


| 环境    | 分支        | 部署方式 | 用途     |
| ------- | ----------- | -------- | -------- |
| dev     | `develop`   | 自动部署 | 开发联调 |
| staging | `release/*` | 自动部署 | 预发验收 |
| prod    | `main`      | 手动审批 | 生产环境 |

### 5.3 制品管理

- **镜像命名规范**：`registry.believe.com/believe/{service-name}:{git-commit-sha}`
- **版本标签**：`latest`（最新）、`v{major}.{minor}.{patch}`（正式版）

---

## 六、配置规范

### 6.1 配置文件层级

```
bootstrap.yml          # Nacos连接配置（优先级最高）
application.yml        # 服务通用配置
application-{env}.yml  # 环境特定配置
Nacos配置中心           # 动态配置（优先级最高）
```

### 6.2 Nacos配置规划


| Namespace | 用途     | 配置集示例                                    |
| --------- | -------- | --------------------------------------------- |
| `dev`     | 开发环境 | `user-service-dev.yaml`                       |
| `test`    | 测试环境 | `user-service-test.yaml`                      |
| `prod`    | 生产环境 | `user-service-prod.yaml`                      |
| `common`  | 公共配置 | `common-redis.yaml`、`common-datasource.yaml` |

### 6.3 配置优先级

```
命令行参数 > Nacos动态配置 > application-{env}.yml > application.yml > bootstrap.yml
```

---

## 七、服务端口规划


| 服务               | 端口  | 管理端口(/actuator) | 说明          |
| ------------------ | ----- | ------------------- | ------------- |
| believe-gateway    | 8080  | 8081                | API网关       |
| believe-auth       | 8101  | 8102                | 认证中心      |
| believe-user       | 8201  | 8202                | 用户中心      |
| Nacos              | 8848  | 9848                | 注册/配置中心 |
| Sentinel Dashboard | 8858  | -                   | 限流控制台    |
| xxl-job-admin      | 8888  | -                   | 任务调度中心  |
| SkyWalking OAP     | 11800 | 12800               | 链路追踪后端  |

---

## 八、部署拓扑

### 8.1 开发环境单机部署

```
┌─────────────────────────────────────────────────┐
│           Docker Compose (单机模式)               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│  │  MySQL  │ │  Redis  │ │  Nacos  │            │
│  │  3306   │ │  6379   │ │  8848   │            │
│  └─────────┘ └─────────┘ └─────────┘            │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│  │Sentinel │ │  xxl-job│ │    ES   │            │
│  │  8858   │ │  8888   │ │  9200   │            │
│  └─────────┘ └─────────┘ └─────────┘            │
└─────────────────────────────────────────────────┘
```

### 8.2 生产环境K8s部署

```
┌─────────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                        │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Ingress-Nginx│  │   Prometheus  │  │    Grafana   │      │
│  │   (LB模式)    │  │   (监控采集)   │  │   (可视化)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │              Service Mesh (可选 Istio)              │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │Gateway  │ │ Auth    │ │ User    │ │ xxx     │          │
│  │ Pod x3  │ │ Pod x2  │ │ Pod x3  │ │ Pod x?  │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
│                                                              │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ MySQL   │ │ Redis   │ │ Nacos   │ │   ES    │          │
│  │(Stateful)│ │(Stateful)│ │(Stateful)│ │(Stateful)│          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
└─────────────────────────────────────────────────────────────┘
```

---

## 九、Maven配置示例

### 9.1 Framework父POM (believe-framework/pom.xml)


| 属性                         | 版本            | 说明                                         |
| ---------------------------- | --------------- | -------------------------------------------- |
| `spring-boot.version`        | 4.0.6           | 核心框架                                     |
| `spring-cloud.version`       | 2025.1.0        | 微服务生态                                   |
| `spring-cloud-alibaba.version` | 2025.1.0.0    | 阿里微服务组件                               |
| `mybatis-plus.version`       | 3.5.15          | ORM增强                                      |
| `shardingsphere.version`     | 5.5.3           | 分库分表                                     |
| `mysql.version`              | 9.3.0           | 数据库驱动                                   |
| `sa-token.version`           | 1.45.0          | 认证授权                                     |
| `xxl-job.version`            | 2.5.0           | 任务调度                                     |
| `knife4j.version`            | 4.5.0           | API文档                                      |

**dependencyManagement 结构**：

1. 导入 BOM：`spring-boot-dependencies` → `spring-cloud-dependencies` → `spring-cloud-alibaba-dependencies`
2. 显式管理不在任何 BOM 中的组件：MyBatis-Plus、MySQL Connector、Sa-Token、xxl-job、Knife4j

### 9.2 believe-common-core/pom.xml

| 依赖 | GroupId | Scope |
|------|---------|-------|
| `jackson-databind` | `tools.jackson.core` | 默认（编译） |
| `jackson-annotations` | `com.fasterxml.jackson.core` | 默认（编译） |
| `lombok` | `org.projectlombok` | provided |

### 9.3 believe-common-log/pom.xml

| 依赖 | 说明 |
|------|------|
| `believe-common-core` | 核心工具包 |
| `spring-aop` | Spring AOP 核心 |
| `aspectjweaver` | AspectJ 切面执行 |
| `jakarta.servlet-api` | Servlet API（provided） |

### 9.4 believe-common-data/pom.xml

| 依赖 | 说明 |
|------|------|
| `believe-common-core` | 核心工具包 |
| `mybatis-plus-spring-boot4-starter` | ORM增强 |
| `mybatis-plus-jsqlparser` | 分页SQL解析 |
| `mysql-connector-j` | MySQL驱动（runtime） |
| `postgresql` | PostgreSQL驱动（runtime） |
| `shardingsphere-jdbc` | 分库分表 |
| `snakeyaml` | YAML解析 |
| `spring-boot-starter` | Spring Boot自动配置 |
| `spring-aop` + `aspectjweaver` | AOP数据源切换 |
| `lombok` | 简化代码（provided） |

### 9.5 各微服务模块的pom.xml

每个独立微服务以 `believe-framework` 为父POM，按需引入 common 模块 + Nacos 服务发现/配置 + `spring-boot-maven-plugin`。

---

## 十、关键版本更新说明

### 10.1 本次更新调整（v2.8）


| 变更项 | 调整前 | 调整后 | 说明 |
|--------|--------|--------|------|
| **统一返回命名** | `R<T>` / `PageR<T>` | **`Result<T>` / `PageResult<T>`** | 更语义化的命名，使用 Lombok `@Data` + `@Accessors(chain=true)` |
| **认证上下文** | 静态方法 `AuthContext.set(key,val)` | **实例式 ThreadLocal + 链式设置** | `AuthContext.get().setUserId().setUsername()`，新增 token/authorities 管理 |
| **请求上下文** | 仅 `traceId` | **实例式 ThreadLocal + 链式设置** | 新增 `clientIp`、`userAgent`，支持 `hasTraceId()` 判断 |
| **数据源架构** | 仅概念描述 | **4种模式完整设计** | 单库/读写分离/多数据源/分库分表，AOP注解切换，条件装配 |
| **BaseEntity** | 无 | **抽象基类** | 统一管理 id/createTime/updateTime/createBy/updateBy/deleted |
| **ShardingSphere** | 无 | **引入 5.5.3** | 分库分表支持，YAML驱动规则配置 |
| **工具类完善** | JsonUtil 为主 | **新增 DateUtil、IdUtil** | 基于 java.time 的日期工具 + UUID/自增ID生成 |
| **Lombok 引入** | 无 | **core + data 模块引入** | 简化样板代码，Result/PageResult/BelieveException/BaseEntity 使用 |

### 10.2 believe-common-data 数据源模式对比


| 模式 | 配置前缀 | 适用场景 |
|------|----------|----------|
| 单数据源 | `spring.datasource`（默认） | 单体应用、开发环境 |
| 读写分离 | `spring.datasource.master` / `spring.datasource.slave` | 读多写少，主从架构 |
| 多数据源 | `spring.datasource.master` / `spring.datasource.slave` / `spring.datasource.report` | 多库独立业务 |
| 分库分表 | `believe.data.sharding.enabled=true` | 大数据量，水平拆分 |

所有模式互斥，通过 `@ConditionalOnProperty` 自动装配，单数据源为默认兜底。

### 10.3 Jackson 3 关键变更说明


| 变更项         | Jackson 2                                        | Jackson 3                                       |
| -------------- | ------------------------------------------------ | ----------------------------------------------- |
| 核心包名       | `com.fasterxml.jackson`                          | **`tools.jackson`**                             |
| 核心类构建     | `new ObjectMapper()`                             | **`JsonMapper.builder().build()`**              |
| 日期格式配置   | `SerializationFeature.WRITE_DATES_AS_TIMESTAMPS` | **`DateTimeFeature.WRITE_DATES_AS_TIMESTAMPS`** |
| 注解包名       | `com.fasterxml.jackson.annotation`               | **保持不变**                                    |
| JavaTimeModule | 需要手动注册                                     | **内置，无需注册**                              |

### 10.4 最终保留的显式版本管理

以下组件**不在** Spring Boot/Cloud 的任何 BOM 中，必须显式管理：


| 组件                          | 版本            | 说明                                         |
| ----------------------------- | --------------- | -------------------------------------------- |
| MyBatis-Plus                  | 3.5.15          | Spring Boot 4 专用 Starter                   |
| ShardingSphere                | 5.5.3           | 分库分表核心                                 |
| Sa-Token                      | 1.45.0          | Spring Boot 4 专用 Starter                   |
| xxl-job                       | 2.5.0           | 第三方任务调度框架                           |
| Knife4j                       | 4.5.0           | 网关专用版本（中央仓库最高可用版本）         |
| MySQL Connector               | 9.3.0           | 数据库驱动                                   |
| Nacos/Sentinel/Seata/RocketMQ | 由 SCA BOM 管理 | 导入`spring-cloud-alibaba-dependencies` 即可 |

### 10.5 版本对照参考


| 框架         | Spring Boot 2.x                | Spring Boot 3.x                 | Spring Boot 4.x                     |
| ------------ | ------------------------------ | ------------------------------- | ----------------------------------- |
| Sa-Token     | `sa-token-spring-boot-starter` | `sa-token-spring-boot3-starter` | `sa-token-spring-boot4-starter`     |
| MyBatis-Plus | `mybatis-plus-boot-starter`    | `mybatis-plus-boot-starter`     | `mybatis-plus-spring-boot4-starter` |

### 10.6 关键变更提醒

**Spring Boot 4.0 核心变更**：

1. **Servlet API 升级**：已升级到 **Servlet 6.1**，包路径为 `jakarta.servlet.*`（不是 `javax.servlet.*`）
2. **Jackson 3 迁移**：Spring Boot 4.0 默认使用 Jackson 3，核心包名变更为 `tools.jackson.*`
3. **AOP Starter 不存在独立版本**：`spring-boot-starter-aop:4.0.6` 在仓库中不存在，应直接引入 `spring-aop` 和 `aspectjweaver`
4. **Jackson 3 日期配置**：必须使用 `DateTimeFeature.WRITE_DATES_AS_TIMESTAMPS` 而非 `SerializationFeature`

---

## 十一、文档版本记录


| 版本 | 日期       | 变更内容                                                                      | 作者         |
| ---- | ---------- | ----------------------------------------------------------------------------- | ------------ |
| v1.0 | 2026-05-18 | 初始版本，基于JDK17+Spring Boot 3.2.x                                         | Believe Team |
| v2.0 | 2026-05-18 | 升级至JDK25+Spring Boot 4.0.6+SCA 2025.1.0.0                                  | Believe Team |
| v2.1 | 2026-05-18 | 重构项目结构：believe-future为根节点                                          | Believe Team |
| v2.2 | 2026-05-18 | 移除根父POM，版本依赖统一由believe-framework管理                              | Believe Team |
| v2.3 | 2026-05-18 | 修复Sa-Token和MyBatis-Plus的Spring Boot 4兼容性问题                           | Believe Team |
| v2.4 | 2026-05-18 | ~~补充Spring生态组件版本（已废弃）~~                                          | Believe Team |
| v2.5 | 2026-05-18 | 精简版本管理：删除不必要的显式版本声明                                        | Believe Team |
| v2.6 | 2026-05-18 | 调整Knife4j版本为4.5.0；AOP依赖改用直接引入核心组件                           | Believe Team |
| v2.7 | 2026-05-18 | Jackson 3 完整集成：JsonUtil 改用 Jackson 3 API，添加核心包说明和版本对照     | Believe Team |
| v2.8 | 2026-05-18 | **数据层重构**：4种数据源模式、BaseEntity、上下文链式API；Result/PageResult重命名、Lombok引入 | Believe Team |

---

*本文档为架构设计文档，不包含具体业务代码和DDL实现。详细实现请参考各模块的开发指南。*
